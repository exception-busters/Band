# BandSpace 종합 개발 로드맵

> **프로젝트 목표**: 실시간 합주가 가능한 종합 음악 협업 플랫폼
> **작성일**: 2025-11-22
> **버전**: 1.0

---

## 📋 핵심 기능 분석

### 1️⃣ 실시간 합주 시스템 (최우선)

**현재 상태**: WebRTC 기본 구조 완성 (70%)

**필요 작업**:
- ✅ P2P 오디오 연결 (완료)
- ⚠️ 오디오 품질 개선 (비트레이트, 샘플레이트)
- ❌ 실시간 믹싱 기능 (개인별 볼륨 조절)
- ❌ 지연시간(레이턴시) 최적화
- ❌ 안정성 개선 (재연결, 에러 핸들링)
- ❌ 모니터링 (입력 레벨 미터, 네트워크 상태)

### 2️⃣ 사용자 편의성 & 접근성

**필요 작업**:
- ❌ 원클릭 입장 (마이크 설정 간소화)
- ❌ 튜토리얼 & 온보딩
- ❌ 마이크/오디오 장치 선택 UI
- ❌ 반응형 디자인 (모바일/태블릿)
- ❌ 접근성 개선 (키보드 네비게이션, 스크린 리더)

### 3️⃣ 커뮤니티 (게시판)

**현재 상태**: UI만 구현 (0%)

**필요 작업**:
- ❌ posts 테이블 생성 (DB)
- ❌ 게시글 CRUD 기능
- ❌ 댓글 시스템
- ❌ 좋아요/북마크
- ❌ 이미지/파일 업로드 (Supabase Storage)
- ❌ 실시간 알림
- ❌ 검색 & 필터링

### 4️⃣ 중고장터

**현재 상태**: 미구현 (0%)

**필요 작업**:
- ❌ marketplace 테이블 설계
- ❌ 상품 등록/수정/삭제
- ❌ 이미지 업로드 (다중)
- ❌ 카테고리 분류 (기타, 앰프, 이펙터, 드럼 등)
- ❌ 거래 상태 관리 (판매중, 예약중, 거래완료)
- ❌ 채팅 시스템 (1:1 문의)
- ❌ 찜하기/북마크
- ❌ 검색 & 필터링

### 5️⃣ 합주 시 실시간 믹싱

**현재 상태**: 기본 WebRTC만 (10%)

**필요 작업**:
- ❌ 참여자별 개별 볼륨 조절
- ❌ 팬(Pan) 조절 (스테레오 배치)
- ❌ EQ/이펙트 (선택사항)
- ❌ 솔로/뮤트 기능
- ❌ 믹스 프리셋 저장/로드
- ❌ 메트로놈 공유
- ❌ 마스터 볼륨

### 6️⃣ 개인 녹음 & 믹싱

**현재 상태**: UI만 구현 (20%)

**필요 작업**:
- ⚠️ MediaRecorder 실제 동작 (부분 완료)
- ❌ 다중 트랙 녹음 (합주 시 각 파트별)
- ❌ 파일 저장 (Supabase Storage)
- ❌ 믹스다운 기능
- ❌ 내보내기 (WAV, MP3)
- ❌ 파형 시각화 (실제)
- ❌ 트랙 편집 (자르기, 페이드 인/아웃)

---

## 🏗️ 단계별 개발 계획

### Phase 1: 핵심 기능 안정화 (2-3주)

> **목표**: 실제로 합주가 가능한 안정적인 시스템 구축

#### 1.1 데이터베이스 스키마 완성

- [ ] rooms 테이블 필드 추가 (bpm, latency_ms, region 제거)
- [ ] posts 테이블 생성 (커뮤니티)
- [ ] marketplace 테이블 생성 (중고장터)
- [ ] recordings 테이블 생성 (녹음 파일)
- [ ] messages 테이블 생성 (1:1 채팅)
- [ ] RLS 정책 설정 (보안)

#### 1.2 실시간 합주 시스템 개선

- [ ] 목업 데이터 제거, DB 전용 사용
- [ ] 오디오 품질 설정 (고음질 옵션)
- [ ] 참여자별 개별 볼륨 조절 UI
- [ ] 입력 레벨 미터 (실제 동작)
- [ ] 재연결 로직 개선
- [ ] 에러 핸들링 강화

#### 1.3 사용자 경험 개선

- [ ] 마이크 권한 요청 플로우 개선
- [ ] 오디오 장치 선택 기능
- [ ] 연결 상태 표시 개선
- [ ] 로딩 상태 스켈레톤 UI

---

### Phase 2: 커뮤니티 & 콘텐츠 (2주)

> **목표**: 사용자 간 소통 활성화

#### 2.1 게시판 기능

- [ ] posts 테이블 연동
- [ ] 게시글 작성/수정/삭제
- [ ] 이미지 업로드 (Supabase Storage)
- [ ] 댓글 시스템
- [ ] 좋아요/조회수
- [ ] 실시간 업데이트 (Supabase Realtime)

#### 2.2 검색 & 필터

- [ ] 게시글 검색
- [ ] 카테고리별 필터링
- [ ] 트렌딩/인기글

---

### Phase 3: 중고장터 (2주)

> **목표**: 악기 거래 플랫폼 구축

#### 3.1 상품 관리

- [ ] 상품 등록/수정/삭제
- [ ] 다중 이미지 업로드
- [ ] 카테고리 시스템
- [ ] 가격/거래 위치 정보

#### 3.2 거래 기능

- [ ] 1:1 채팅 시스템 (Supabase Realtime)
- [ ] 거래 상태 관리
- [ ] 찜하기/북마크
- [ ] 판매자 프로필

---

### Phase 4: 녹음 & 믹싱 (3주)

> **목표**: 전문적인 음악 제작 기능

#### 4.1 개인 녹음

- [ ] MediaRecorder 완전 구현
- [ ] 파일 저장 (Supabase Storage)
- [ ] 녹음 목록 관리
- [ ] 파형 시각화 (실제)

#### 4.2 합주 녹음

- [ ] 다중 트랙 녹음 (참여자별)
- [ ] 믹스다운 기능
- [ ] 개별 트랙 다운로드

#### 4.3 믹싱 기능

- [ ] 트랙별 볼륨/팬 조절
- [ ] 기본 이펙트 (리버브, 딜레이)
- [ ] 내보내기 (WAV, MP3)

---

### Phase 5: 고급 기능 & 최적화 (2-3주)

> **목표**: 사용자 경험 극대화

#### 5.1 합주 고급 기능

- [ ] 메트로놈 공유
- [ ] 세션 녹화 (전체)
- [ ] 리허설 모드 (녹음 없이)
- [ ] 방 설정 프리셋

#### 5.2 UI/UX 개선

- [ ] 반응형 디자인 완성
- [ ] 다크모드
- [ ] 접근성 개선
- [ ] 온보딩 튜토리얼

#### 5.3 성능 최적화

- [ ] 이미지 최적화
- [ ] 코드 스플리팅
- [ ] 캐싱 전략
- [ ] 번들 사이즈 최적화

---

## 🎯 우선순위

### 긴급 (지금 바로)

1. ✅ 데이터베이스 스키마 완성
2. ✅ 목업 데이터 제거
3. ✅ 실시간 합주 안정화

### 중요 (1개월 내)

4. 개인별 볼륨 조절
5. 커뮤니티 게시판 완성
6. 녹음 기능 실제 동작

### 보통 (2-3개월 내)

7. 중고장터 구축
8. 합주 녹음
9. 믹싱 기능

### 나중에 (3개월 이후)

10. 고급 이펙트
11. 모바일 최적화
12. 추가 기능들

---

## 📊 필요한 기술 스택 추가

현재 있는 것 외에 추가로 필요한 라이브러리:

```json
{
  "wavesurfer.js": "파형 시각화",
  "tone.js": "오디오 이펙트/메트로놈",
  "react-dropzone": "파일 업로드",
  "date-fns": "날짜 처리",
  "react-virtualized": "긴 목록 최적화",
  "socket.io": "1:1 채팅 (선택사항)"
}
```

---

## 🗂️ 데이터베이스 스키마 설계

### profiles (사용자 프로필)

```sql
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  email TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  nickname TEXT NOT NULL UNIQUE,
  birth_date DATE NOT NULL,
  gender TEXT,
  phone TEXT,
  avatar_url TEXT,
  bio TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### rooms (합주실)

```sql
CREATE TABLE rooms (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  description TEXT,
  host_id UUID REFERENCES profiles(id) NOT NULL,
  max_participants INTEGER NOT NULL DEFAULT 8,
  current_participants INTEGER DEFAULT 0,
  status TEXT CHECK (status IN ('open', 'recording', 'locked')) DEFAULT 'open',
  genre TEXT,
  tags TEXT[],
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### posts (커뮤니티 게시글)

```sql
CREATE TABLE posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  author_id UUID REFERENCES profiles(id) NOT NULL,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  category TEXT CHECK (category IN ('session', 'collab', 'beta', 'feedback', 'general')),
  tags TEXT[],
  images TEXT[],
  likes_count INTEGER DEFAULT 0,
  comments_count INTEGER DEFAULT 0,
  views_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### comments (댓글)

```sql
CREATE TABLE comments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  post_id UUID REFERENCES posts(id) ON DELETE CASCADE NOT NULL,
  author_id UUID REFERENCES profiles(id) NOT NULL,
  content TEXT NOT NULL,
  parent_id UUID REFERENCES comments(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### marketplace (중고장터)

```sql
CREATE TABLE marketplace (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  seller_id UUID REFERENCES profiles(id) NOT NULL,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  category TEXT CHECK (category IN ('guitar', 'bass', 'drum', 'keyboard', 'amp', 'effect', 'mic', 'other')),
  price INTEGER NOT NULL,
  location TEXT,
  images TEXT[],
  status TEXT CHECK (status IN ('available', 'reserved', 'sold')) DEFAULT 'available',
  views_count INTEGER DEFAULT 0,
  likes_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### recordings (녹음 파일)

```sql
CREATE TABLE recordings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) NOT NULL,
  room_id UUID REFERENCES rooms(id),
  title TEXT NOT NULL,
  description TEXT,
  file_url TEXT NOT NULL,
  file_size INTEGER,
  duration INTEGER,
  format TEXT,
  is_public BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### messages (1:1 채팅)

```sql
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sender_id UUID REFERENCES profiles(id) NOT NULL,
  receiver_id UUID REFERENCES profiles(id) NOT NULL,
  content TEXT NOT NULL,
  is_read BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🔒 보안 고려사항

### RLS (Row Level Security) 정책

1. **profiles**: 본인만 수정 가능, 모두 읽기 가능
2. **rooms**: 모두 읽기 가능, 인증된 사용자만 생성, 호스트만 수정/삭제
3. **posts**: 모두 읽기 가능, 인증된 사용자만 작성, 작성자만 수정/삭제
4. **marketplace**: 모두 읽기 가능, 인증된 사용자만 작성, 판매자만 수정/삭제
5. **recordings**: 본인 녹음만 조회/삭제, public인 경우 모두 읽기 가능
6. **messages**: 송신자와 수신자만 조회 가능

### 환경 변수 관리

```env
# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# WebSocket 시그널링 서버
VITE_SIGNALING_URL=ws://localhost:8080

# 프로덕션 환경
VITE_SIGNALING_URL_PROD=wss://your-domain.com
```

---

## 📈 성능 목표

### 오디오 레이턴시

- 목표: 50ms 이하 (로컬 네트워크)
- 허용: 100ms 이하 (인터넷)

### 동시 접속

- 합주실당: 2-8명 (P2P)
- 향후: SFU 도입 시 20명+

### 페이지 로드 속도

- FCP (First Contentful Paint): 1.5초 이하
- LCP (Largest Contentful Paint): 2.5초 이하
- TTI (Time to Interactive): 3.5초 이하

---

## 🚀 배포 계획

### 개발 환경

- Frontend: localhost:5173 (Vite)
- Backend: localhost:8080 (WebSocket)
- Database: Supabase Cloud

### 스테이징 환경

- Frontend: Vercel/Netlify
- Backend: Render/Railway
- Database: Supabase (별도 프로젝트)

### 프로덕션 환경

- Frontend: Vercel/Cloudflare Pages
- Backend: AWS/GCP (낮은 레이턴시)
- Database: Supabase Pro
- CDN: Cloudflare
- Monitoring: Sentry

---

## 📝 개발 프로세스

### 브랜칭 전략

```
main (프로덕션)
  ↓
develop (개발)
  ↓
feature/* (기능별 브랜치)
```

### 커밋 메시지 규칙

```
feat: 새 기능 추가
fix: 버그 수정
refactor: 리팩토링
style: 스타일 변경
docs: 문서 수정
test: 테스트 추가
chore: 기타 작업
```

---

## 🎓 참고 자료

### WebRTC

- [WebRTC 공식 문서](https://webrtc.org/)
- [MDN WebRTC API](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)

### Supabase

- [Supabase 공식 문서](https://supabase.com/docs)
- [Realtime 가이드](https://supabase.com/docs/guides/realtime)

### Web Audio API

- [MDN Web Audio API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API)
- [Tone.js 공식 문서](https://tonejs.github.io/)

---

## 📞 문의 및 피드백

프로젝트 관련 문의나 제안사항은 GitHub Issues를 통해 남겨주세요.

---

**최종 수정일**: 2025-11-22
**문서 버전**: 1.0
