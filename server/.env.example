# Supabase 연결 설정 (서버에서 참여자 수 동기화에 사용)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# 서버 포트 (기본값: 8080)
PORT=8080

# ========== Demucs 음원 분리 설정 ==========
# 프로바이더 선택: local | huggingface | replicate
# - local: 로컬 Python 실행 (개발용, GPU 있으면 빠름)
# - huggingface: Hugging Face Spaces (무료, 배포용)
# - replicate: Replicate API (유료, 프로덕션용)
DEMUCS_PROVIDER=local

# Hugging Face Spaces URL (huggingface 프로바이더 사용 시)
HF_SPACE_URL=https://facebook-demucs.hf.space

# Replicate API Token (replicate 프로바이더 사용 시)
# https://replicate.com 에서 발급
REPLICATE_API_TOKEN=your-replicate-token
